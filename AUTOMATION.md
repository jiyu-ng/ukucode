# 우쿨코드 🎸 — 자동 기능개선 가이드

우쿨코드는 우쿨렐레 코드·노래 사전 **광고 수익형** 정적 사이트(Astro, GitHub Pages `jiyu-ng.github.io/ukucode`).
키리의 로테이션 자동개선 크론이 이 파일을 기준으로 **작은 개선 1건**을 골라 구현한다.

## 성격·우선순위

- **광고수익 사이트** → 검색 유입(SEO)·체류·광고 노출이 수익의 핵심.
- 우선순위: **SEO · 수익화 · UX · 성능 · 접근성 · 코드품질** > 그다음 아래 ROADMAP.
- 콘텐츠(새 코드·노래 데이터 추가)는 이 크론의 일이 **아님**(별도). 기능·구조·품질 개선만.

## ✅ 자동 머지 OK (빌드 통과 시 바로 squash 머지)

서스테이닝·소규모 개선:
- **SEO/노출**: 메타 설명, JSON-LD, sitemap, robots, OG 태그, canonical, 내부 링크, 코드/노래 목록 구조화
- **접근성**: aria, 색 대비, 시맨틱 마크업, 키보드 내비, 포커스
- **UI/스타일**: 레이아웃 안 깨지는 선의 디자인 정리·간격·반응형(특히 모바일에서 코드 다이어그램 가독성)
- **성능**: 이미지/폰트 최적화, lazy load, 번들 소소한 정리
- **유지보수**: 의존성 패치 업데이트, 린트/포맷, 죽은 코드 제거, 주석, 오타

**공통 조건**: `npm run build` 통과 + 기존 페이지 안 깨짐 + 변경 범위 작음. 하나라도 불충족 → 리뷰行(PR만).

## 🙋 리뷰行 (머지 X, PR 열어두고 DM 보고)

- 리팩토링/디렉토리 구조 변경, 새 라우트/페이지 타입, 데이터 모델 변경
- 디자인·브랜드 인상 바뀌는 변화
- 빌드/CI/배포/env, 의존성 major 업데이트
- **수익화**: 애드센스·광고 코드 (있다면)
- 그 외 조금이라도 애매하면 전부 리뷰行

## 작업 흐름

1. `git checkout main && git pull`
2. 개선 1건 구현 + `npm run build`(base `/ukucode` 유지) 통과 확인
3. `auto/feat-<slug>` 브랜치 커밋·push, `gh pr create` (base main)
4. 위 기준으로 자동머지 or 리뷰行 판단 → DM 보고
5. 마땅한 개선거리 없으면 조용히 패스(보고 생략)

## 🙋 대표님 판단 대기 — 4건 (2026-09-10 기준)

> ⚠️ **이건 「PR 이 밀린 것」이 아니다. 열린 PR 은 0건이고, 아래는 전부 로드맵 단계의 선택지다.**
> 앞 슬롯이 "리뷰行이 4건 밀려 있다"로만 적어둬서 **PR 적체로 오독된다**(2026-09-10 실제로 오독했다).
> 확인 방법: `gh pr list --state open` — 0 이면 코드로 막힌 건 없다.

| # | 건 | 성격 | 왜 자동으로 못 하나 |
|---|---|---|---|
| 1 | `og:image` 없음 (0/55) | SEO·공유 | 1200×630 브랜드 이미지가 필요 — **브랜드 인상** |
| 2 | 곡 상세 가사 위 코드 링크 10×17 | 접근성 | 패딩으로 못 고침 · **오작동 위험**(인접 코드 겹침) |
| 3 | 목록 검색·필터 UX | UX | **새 기능** — 범위가 큼 |
| 4 | `--accent` 텍스트 대비 3.49 | 접근성 | **브랜드 색**을 바꾸는 일 |

📌 **넷 다 「구현이 어려워서」가 아니라 「내가 정할 일이 아니라서」 멈춰 있다.** 상세·선택지는 아래 각 항목에.

## ROADMAP (개선 백로그 — 처리하면 [x], 새 아이디어 추가)

- [x] `<head>` SEO 점검 (2026-08-30 실측): title·description·canonical·og:title·twitter:card **56/56 전 페이지 정상**.
      Base.astro 에 중앙화돼 있어 `src/pages/` 만 grep 하면 0건으로 보이니 주의 — **dist 로 확인할 것**.
- [x] 404 페이지 `noindex, follow` (2026-08-30). 이전엔 `index, follow` 라 색인 시 soft-404 신호가 됐다.
> ⚠️ **`[ ]` 를 「안 됐다」로 읽지 말 것.** 2026-08-31 슬롯이 아래 다섯 항목을 재확인하는 데 통째로
> 쓰였고, **전부 이미 구현돼 있었다.** 로드맵은 구현보다 늦게 낡는다 —
> **손대기 전에 `dist` 로 실측**하고, 끝난 건 결과를 숫자로 적어 닫는다.

- [ ] **og:image 없음 (0/55, 2026-08-31 재확인)** — 카톡·트위터 공유 시 썸네일이 안 뜬다. 1200×630 브랜드
      이미지 1장 + `og:image`·`twitter:card=summary_large_image`. ⚠️ 브랜드 인상이 걸려 **리뷰行**
- [x] 🔴 **thin content — 대표님 판단: 그냥 둠 (2026-08-31)**. 선택지 ①설명 추가 ②곡 늘리기 ③보류 를
      드렸고 **③ 보류**를 고르셨다. **알고도 유지**지 모르는 문제가 아니다 — **다시 발견해도 재제기하지 말 것.**
      (아래는 그때 실측. 유입이 살아나면 대표님이 먼저 말씀하신다)
- [x] ~~thin content — 코드 페이지 본문이 평균 216~269자~~ (2026-08-31 실측, 49페이지 전수).
      노래 링크 있는 14개는 269자, 없는 35개는 216자. 최소 `/chord/b` 191자.
      **광고 수익 사이트라 SEO·애드센스 양쪽에 걸리는 자리**인데, 코드별 설명을 붙이는 건
      **콘텐츠 결정이라 이 크론 범위 밖**이다(가이드: 콘텐츠 추가 X). ⚠️ **대표님 판단 필요**
      측정: `dist/chord/*/index.html` 에서 script·style 제거 후 태그 벗기고 글자수
- [x] **터치 대상 크기(WCAG 2.5.8, 최소 24×24)** (2026-09-08 실측·부분 수정).
      390×844 모바일 뷰포트에서 playwright 로 클릭 가능 요소를 전수 측정했다.
      ```
      홈        53개 중 미달 0
      노래목록   8개 중 미달 0
      코드상세  14개 중 미달 1  → 「← 전체 코드」 69×17   🔧 고침 → 81×30
      곡상세    20개 중 미달 13 → 가사 위 코드 링크 10×17  ⚠️ 아래 참조
      ```
      → 뒤로가기(`.crumb a`)는 `inline-block` + 세로 패딩으로 25px 확보. **음수 마진으로 좌측 정렬 유지.**
      `chord/[slug].astro` 와 `song/[slug].astro` **둘 다** 같은 패턴이라 함께 고쳤다.
- [ ] 🔴 **곡 상세 — 가사 위 코드 링크가 10×17 (24px 미달, 13개/페이지). 리뷰行**
      우쿨렐레 곡 페이지에서 **코드를 눌러 운지를 보는 게 핵심 동선**인데 손가락으로 누르기 어렵다.
      🔴 **단순 패딩으로 못 고친다** — 구조가 이렇다.
      ```
      .line      display:flex; align-items:flex-end
      .seg       display:inline-flex; flex-direction:column   (코드 위 / 가사 아래)
      .seg-chord height:20px 고정 · font-size:14px
      ```
      세로 패딩 → `.seg-chord` 20px 고정을 넘겨 **가사 줄 간격이 벌어진다**.
      가로 패딩 → **코드가 붙어 있어야 할 음절 위치와 어긋난다**.
      선택지 ①`::after` 로 시각 변화 없이 터치 영역만 확장(⚠️ 인접 코드끼리 영역이 겹치면
      **엉뚱한 코드로 이동해 지금보다 나빠질 수 있다** — 겹침 실측 필요)
      ②코드 줄 높이를 키움(줄 간격 증가 감수) ③현행 유지. **대표님 판단 필요.**

- [x] **코드 상세 다이어그램이 카드 가용 폭을 다 안 썼다** (2026-09-11 실측·수정).
      AUTOMATION.md 가 자동머지 범주로 **명시한** 「반응형 — 특히 모바일에서 코드 다이어그램
      가독성」인데 실측 기록이 없어서 쟀다. 이 축은 **닫혔다.**
      ```
      뷰포트 390 / 768 / 1280  →  다이어그램 전부 156×192 로 동일 (반응형 아님)
      원인  ChordDiagram.astro:31 이 width={92*size} height={113*size} 로 속성 고정.
            viewBox 가 있어 스케일은 가능한데 폭이 안 풀려 컨테이너를 안 따라감
      카드  .diagram-wrap max-width 280 − 테두리 2 − 좌우 패딩 44 = 가용 234px
      결과  234px 중 156px(66%)만 쓰고 나머지 78px 는 여백으로 버려짐
      ```
      🔧 `.diagram-wrap :global(.chord-diagram) { width:100%; height:auto }` **한 줄.**
      **156 → 234px (+50%)** · 카드 폭·레이아웃은 그대로(높이만 192→287).
      ⚠️ 컴포넌트가 Astro scoped 라 `:global()` 없이는 안 먹는다.
      ✅ 회귀 확인: **목록 썸네일 75px ×49개 그대로** · `aria-hidden` 49개 그대로 · 빌드 56p.
      📌 **기대값을 먼저 적어둔 게 값을 했다** — 236px 로 기대했는데 234가 나와 `border 2px`
      를 빼먹은 걸 잡았다. **주석에 적어둔 숫자도 같이 틀려 있어서** 함께 고쳤다.
      ⚠️ `grep -c 'aria-hidden'` 이 **1** 을 줬다 — minify 라 한 줄이다. `grep -o … | wc -l` 로 셀 것.

- [ ] 코드/노래 목록 검색·필터 UX (검토는 리뷰行)

- [x] **인쇄 스타일 · 다크모드 · 포커스 · 모션 축** (2026-09-09 실측). 새 축을 찾으려고 넷을 쟀다.
      ```
      focus-visible            있음 (1)      ✅ 이미 구현
      prefers-reduced-motion   있음 (1)      ✅ 이미 구현
      prefers-color-scheme     없음          → 다크모드. 따뜻한 베이지가 브랜드라 인상이 바뀐다 · 범위 큼
      @media print             없음          → 인쇄 시 내비·푸터·스킵링크가 그대로 나오고
                                              화면 높이와 인쇄 높이가 같다(곡 985 · 코드 1218)
      ```
      🔴 **둘 다 만들지 않았다.** 인쇄는 「우쿨렐레 코드를 종이에 뽑아 본다」가 그럴듯하지만 **수요 근거가 없고**,
      내비·푸터가 한 줄씩 더 나오는 정도라 **실질 이슈라 부르기 약하다.** 다크모드는 브랜드 인상이 걸려 리뷰行인데
      **대표님 판단 대기가 이미 4건이다**(위 표. PR 적체가 아니라 결정 대기다). 가이드의 「억지 개선 X」에 걸린다고 봤다.
      📌 인쇄 수요가 확인되면 그때가 근거다 — 지금은 **내 추정**뿐이다.

      ⚠️ **측정에서 내가 세 번 0을 냈고 세 번 다 「없음」이 아니었다.**
      ```
      dist/_astro/*.css        → 그런 경로가 없다 (CSS 는 <style> 로 인라인)
      grep '<style...>.*</style>'  → 여러 줄에 걸쳐 있어 한 줄 정규식이 못 잡는다
      ✅ 되는 법: python re.findall(r'<style[^>]*>(.*?)</style>', h, re.S) 로 뽑아서 센다 (2,867자)
      ```
      **0 이 나오면 원문을 연다.** 이 저장소는 CSS 가 파일로 안 떨어져서 특히 잘 속는다.
      ⚠️ 로컬 확인은 `dist` 를 그냥 서빙하면 404 다 — `base: '/ukucode'` 라 **`<루트>/ukucode/` 로 심볼릭 링크**를 걸어야 한다.

> 📌 **2026-09-05 기준, 자동머지 가능한 개선거리는 고갈됐다.** 위 두 개(`og:image`·검색 UX)만
> 남았고 **둘 다 대표님 판단이 필요한 리뷰行**이다. 다음 슬롯은 **새 축을 찾거나 조용히 패스**한다 —
> 이미 닫힌 축을 다시 재는 데 슬롯을 쓰지 말 것.

- [x] **색 대비(WCAG AA)** (2026-09-06 실측·수정). `--muted #8a7f78` 이 배경 대비 **3.65** 로
      AA(4.5) 미달이었고, 쓰이는 자리가 **`.site-nav a` 14px · `.tagline` 13.5px · `.site-foot` 12.5px**
      로 전부 **일반 텍스트**였다(큰 텍스트 예외 3.0 이 적용 안 되는 크기).
      → **`#7a706a` 로 명도만 낮춤**(색상·채도 유지) · bg 4.52 / card 4.82 ✅.
      다이어그램의 `{n}fr` 라벨(11px)도 같은 색이라 같이 바꿨다.
      측정: 빌드 산출물의 `:root` 변수를 뽑아 WCAG 상대휘도 공식으로 계산.
      ⚠️ **개방현 표시 원의 `stroke` 는 안 바꿨다** — 그래픽 요소는 기준이 3.0 이라 3.90 으로 통과다.

- [ ] 🟡 **`--accent #e8503a` 가 텍스트로 쓰이는 자리 — 대비 3.49 (AA 미달), 리뷰行**
      `.site-nav a:hover` · `.site-nav a[aria-current=page]` 가 14px 상속이라 일반 텍스트다.
      **브랜드 색이라 자동으로 안 바꿨다.** 선택지 ①accent 를 어둡게(브랜드 인상 변화)
      ②텍스트용 변수를 따로 두고 내비에만 적용 ③현행 유지(장식으로 간주). **대표님 판단 필요.**
      📌 배경으로 쓰는 자리(`.skip-link`)와 도트(`circle fill`)는 **그래픽이라 문제 없다.**

- [x] **RSS 피드 — 만들지 않는다** (2026-09-07 판정). 지금 없고, **없는 게 맞다.**
      RSS 는 **새 글이 계속 나오는 사이트**용인데 우쿨코드는 코드·노래 **사전**이라 콘텐츠가 거의 안 늘고,
      콘텐츠 추가는 이 크론 범위 밖이다. 추가하면 **갱신 안 되는 빈 피드**가 되고 구독자에게 아무것도 안 간다.
      📌 옆 프로젝트(옳은경제)엔 RSS 가 있어서 **"여긴 왜 없지"로 보이기 쉽다** — 사이트 성격이 다르다.
- [x] **이미지·외부 링크** → 🟢 **점검 대상 자체가 없다** (2026-09-07, dist 56 전수).
      `<img>` **0개**(코드 다이어그램은 인라인 SVG) · 외부 링크 **0개**.
      그래서 `loading`·`fetchpriority`·`noopener` 는 **볼 태그가 없다.** 없는 걸 넣지 말 것.

### ✅ 끝난 것 (다시 열지 말 것 — 확인 방법 포함)

- [x] `<head>` SEO (2026-08-30): title·description·canonical·og:title·twitter:card 전 페이지 정상.
      `Base.astro` 에 중앙화돼 있어 `src/pages/` 만 grep 하면 0건으로 보인다 — **dist 로 확인.**
- [x] 404 `noindex, follow` (2026-08-30). 이전엔 `index, follow` 라 색인 시 soft-404 신호였다.
- [x] **sitemap + robots.txt** (2026-08-31 실측). `astro.config` 에 `@astrojs/sitemap` 통합돼 있고
      `dist/sitemap-0.xml` **55 URL = dist 페이지 55개, 누락 0**. base `/ukucode` 정상.
      ⚠️ `grep -c '<loc>'` 는 **1** 을 준다 — XML 이 한 줄이라 `-c` 가 줄을 센다. `grep -o … | wc -l` 로 셀 것.
- [x] **ChordDiagram 접근성** (2026-08-31). `role="img"`+`aria-label`, 이름 없으면 `aria-hidden`
      (목록에서 옆 텍스트가 코드명을 읽어줘 중복 낭독 방지). 의도까지 주석에 적혀 있다.
- [x] **내부 링크 상호 연결** (2026-08-31). `chord/[slug].astro` 가 `usedInSongs` 로 「이 코드로 연주하는
      노래」 섹션을 만든다. 49개 중 **14개에 노래 링크**(4곡이 쓰는 코드가 14종이라 그렇고, 나머지 35개는
      섹션이 안 뜨는 게 정상). ⚠️ **표본을 `/chord/a/` 로 잡으면 0건이라 「미구현」으로 오판한다** — 실제로
      쓰이는 코드(`c`·`f`·`g`)로 확인할 것.
- [x] **구조화 데이터** (2026-08-31). 55/55 페이지에 JSON-LD.
      `BreadcrumbList 54 · HowTo 49 · HowToStep 196 · ItemList 2 · WebSite 1`.
- [x] **폰트/이미지 로딩** (2026-08-31). **추가 최적화 여지 없음** — 외부 오리진 참조가 **0개**라
      `preconnect` 를 붙일 데가 없고(`schema.org`·`w3.org` 는 네임스페이스 문자열), `<img>` 도 0개라
      lazy load 대상이 없다. **없는 걸 넣지 말 것.**
- [x] **메타태그 전수 실측** (2026-09-04, dist 56페이지). description 누락 0 · canonical 누락 0 ·
      중복 title 0 · 중복 description 0 · title 60자 초과 0 · description 160자 초과 0(최장 111자) ·
      JSON-LD 파싱 실패 0. **이 축은 더 팔 게 없다** — 다음 슬롯에서 재점검하지 말 것.
      측정: `dist/**/index.html` 을 파싱해 title·description·canonical 추출 + `json.loads` 로 LD 검증.
- [x] **내부 링크 그래프 실측** (2026-09-05, dist 55페이지). **고아 페이지 0건.**
      코드 49페이지 전부 outbound **11~14개**(관련 코드 8 + 내비), inbound **최소 1**.
      `chord/[slug].astro:26` 의 `related`(같은 루트 or 같은 품질, 최대 8)가 이미 상호 연결을 만든다.
      ⚠️ **「노래 링크가 35개에 없다」를 링크 부족으로 읽지 말 것** — 관련 코드 링크가 따로 있어
      막다른 페이지가 아니다. **이 축은 더 팔 게 없다.**
      측정: dist 전 HTML에서 `href="/ukucode/…"` 수집 → 자기 자신 제외 후 inbound/outbound 집계
- [x] **robots·favicon·번들** (2026-09-05 실측). `robots.txt` Sitemap 라인 정상(`sitemap-index.xml`),
      sitemap `<loc>` **55개 = dist 페이지 55개**, `favicon.svg` 참조·실파일 둘 다 존재(404 아님),
      404 `noindex, follow` 유지, dist 총 **848K**(별도 JS/CSS 번들 없음 — 인라인). **최적화 여지 없음.**
      ⚠️ `<loc>` 는 `grep -o '<loc>' | wc -l` 로 셀 것(`grep -c` 는 1).

- [x] **접근성 기본** (2026-08-31). `lang`·skip link·`<main>`·`:focus-visible`·`h1`·`theme-color` 전부 있음.
      헤딩 순서 `h1→h2→h2→h2` 정상.
